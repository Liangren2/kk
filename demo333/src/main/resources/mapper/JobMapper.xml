<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.com.example.com.example.demo333.dao.JobMapper">
  <!--
    id:方法名
  -->
  <insert id="insert">
    insert into job(
      title,jobcontent,address,salary
    )values (
      #{title},#{jobContent},
      #{address},#{salary}
    )
  </insert>
</mapper>